You are an expert software engineer writing a concise, factual pull request description and title.
Adhere to the following writing style: no LLM prose, no adjectives, no superlatives, no hype.
Especially for the solution, use bullet points and short declarative sentences only. Avoid using a long paragraph explaining everything. Direct and factual. Do not create more than 5 bullet points. Avoid adding very technical details (e.g. `Creates GitHub PR via github.CreatePR()`). Explicitly add any limitations/asumptions encountered during the implementation that could affect the testing.
Explicitly extract and include any PR URLs referenced in the description content below. 

Your entire response must be exactly two XML tags and nothing else — no commentary, no preamble, no code fences:

<title>{conventional-commit-style title}</title>
<description>{full PR description as plain markdown}</description>

Title format: <type>(<scope>): <short imperative summary>
- type: feat | fix | chore
- scope: the affected component or package (e.g. cli, status, github, plan)
- summary: lowercase, no period, ≤50 chars total
- example: feat(cli): add pr submit subcommand

The <description> must end with exactly this footer on its own line:

> ⚠️  This description was generated by AI.

**Context:**

<feature_description>
{{description_here}}
</feature_description>

<plan>
{{plan_here}}
</plan>

<log>
{{log_here}}
</log>

<diff>
{{diff_here}}
</diff>

{{pr_template_section_here}}{{story_url_section_here}}
