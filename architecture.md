Create a new headless prompt `headless/session/execute_slice.md`.
This prompt will be a modified version of `headless/session/execute_task.md`.
The key difference is that it will instruct the LLM to iterate over a series of tasks within a "slice" rather than executing a single task.
It will use the same placeholder system as the existing headless prompts.
